# PyTorch checkpoints (training / ensemble weights)
*.pt

# Model output folders (avoid committing training weights/artifacts)
**/models/
**/ensemble_models/

# INF walk-forward outputs
INF/outputs/**
!INF/outputs/.gitkeep
